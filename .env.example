# API base for the RGS backend. Empty = same origin (nginx proxies /api/*).
# For local dev, point at the RGS server, e.g. http://localhost:8080
NEXT_PUBLIC_RGS_API_BASE=

# Optional: override which hilo variant to use. Defaults to hilo_965 (96.5% RTP).
# NEXT_PUBLIC_HILO_GAME_UUID=hilo_965
